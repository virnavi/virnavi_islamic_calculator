
void main() {
/*  group('SunTimetableCalculator', () {
    test('calculates sun timetable correctly for a given date and location',
        () {
      final date = DateTime.utc(2025, 9, 22);
      const latitude = 34.0522;
      const longitude = -118.2437;

      final timetable = SunTimeTable.calculate(
        dateTime: date,
        latitude: latitude,
        longitude: longitude,
      );

      expect(timetable.solarNoon.hour, 19);
      expect(timetable.solarNoon.minute, closeTo(54, 2));
      expect(timetable.sunrise.from.hour, 13);
      expect(timetable.sunrise.from.minute, closeTo(47, 2));
      expect(timetable.sunset.to.hour, 1);
      expect(timetable.sunset.to.minute, closeTo(40, 2));
    });
  });*/
}
